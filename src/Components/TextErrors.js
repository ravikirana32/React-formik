import React from 'react';
//import '../App.css';

const TextErrors=(props) =>{
    return (
        <div className="error">
            {props.children}
        </div>
    )
}

export default TextErrors
